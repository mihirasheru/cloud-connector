# COSMOS AI - Cloud Connector

This module is responsible for translating common security policies into vendor-specific configurations for:

- AWS Security Groups
- Azure Network Security Groups (NSGs)
- pfSense Firewall Rules

It exposes REST APIs that can be consumed by the COSMOS AI backend.

Current Status:
- Project initialized
- FastAPI installed
- AWS SDK (boto3) installed
- Azure SDK (coming soon)